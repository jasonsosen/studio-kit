# Studio Kit

AIを活用したコンテンツ管理デスクトップアプリ

## 機能

- カレンダーで投稿スケジュール管理
- AIによる文章自動生成（Claude / OpenAI対応）
- 週間プラン一括作成
- 動画・画像素材の管理とサムネイル表示
- **AI字幕自動生成**（Whisperで音声認識）
- API使用量トラッキング（日本円換算）
- Mac起動時に自動起動

---

## インストール方法

### ステップ1: ダウンロード

1. [Releases ページ](https://github.com/jasonsosen/studio-kit/releases/latest) を開く
2. **`Studio.Kit_x.x.x_aarch64.dmg`** をクリックしてダウンロード

### ステップ2: インストール

1. ダウンロードした `.dmg` ファイルをダブルクリック
2. 開いたウィンドウで **Studio Kit** を **Applications** フォルダにドラッグ

### ステップ3: 初回起動

> ⚠️ App Store以外のアプリなので、初回は特別な開き方が必要です

1. **Finder** → **アプリケーション** フォルダを開く
2. **Studio Kit** を **右クリック**（またはControlを押しながらクリック）
3. メニューから **「開く」** を選択
4. 「開いてもよろしいですか？」と聞かれたら **「開く」** をクリック

※ 2回目以降は普通にダブルクリックで開けます

### ステップ4: 初期設定

1. アプリが起動したら、右上の **⚙️ 設定** をクリック
2. **スタジオ名**、**特徴**、**ターゲット** を入力
3. **Claude API Key** を入力（[取得はこちら](https://console.anthropic.com/)）
4. **保存** をクリック

---

## 使い方

### 投稿を作成する

1. カレンダーの日付をクリック
2. トピックを入力して **保存**
3. **🤖 AIで生成** をクリック → キャプションが自動生成されます

### 週間プランを作成する

1. **🤖 週間プラン生成** をクリック
2. AIが今週のトピックを7つ提案
3. 各投稿を編集してキャプションを生成

### 素材を追加する

1. 投稿を開いて編集画面に入る
2. 「素材」セクションで **クリックして選択** をクリック
3. 動画（mp4, mov）または画像（jpg, png）を選ぶ
4. 自動でサムネイルが生成され、カレンダーにも表示されます

> 💡 動画サムネイル生成には **ffmpeg** が必要です（下記参照）

### AI字幕を生成する

1. 動画をアップロードした投稿を開く
2. 「AI字幕」セクションで言語を選択（日本語/英語/中国語/韓国語）
3. **🎤 字幕を生成** をクリック
4. 数分待つとSRT字幕ファイルが生成されます

> 💡 字幕生成には **Whisper** が必要です（下記参照）

### AIプロバイダーを切り替える

1. **⚙️ 設定** を開く
2. **AIプロバイダー** で Claude または OpenAI を選択
3. 対応するAPIキーを入力して保存

---

## 前提条件

### ffmpeg（動画サムネイル用）

動画のサムネイル自動生成を使う場合は、ffmpegをインストールしてください：

```bash
# Homebrewがない場合は先にインストール
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# ffmpegをインストール
brew install ffmpeg
```

### Whisper（AI字幕生成用）

動画から自動で字幕を生成する場合は、OpenAIのWhisperをインストールしてください：

```bash
# Python 3.8以上が必要
pip install openai-whisper

# または pipx を使う場合
pipx install openai-whisper
```

> ⚠️ 初回実行時にモデル（約150MB）がダウンロードされます

---

## 開機自動起動を設定する（オプション）

Macを起動するたびにアプリを自動で開きたい場合：

1. **システム設定** を開く
2. **一般** → **ログイン項目** を選択
3. **+** ボタンをクリック
4. **アプリケーション** から **Studio Kit** を選んで追加

---

## トラブルシューティング

### 「開発元を検証できません」と表示される

→ ステップ3の「右クリック → 開く」の方法で開いてください

### APIキーが正しいのに生成できない

→ [Anthropic Console](https://console.anthropic.com/) または [OpenAI Platform](https://platform.openai.com/) でAPIキーの残高を確認してください

### 動画のサムネイルが生成されない

→ ffmpegがインストールされているか確認してください：
```bash
which ffmpeg
# /opt/homebrew/bin/ffmpeg と表示されればOK
```

インストールされていない場合：
```bash
brew install ffmpeg
```

### 字幕が生成されない

→ Whisperがインストールされているか確認してください：
```bash
which whisper
# パスが表示されればOK
```

インストールされていない場合：
```bash
pip install openai-whisper
```

### 素材がアップロードできない

→ アプリに必要な権限が付与されているか確認してください：
1. **システム設定** → **プライバシーとセキュリティ** → **ファイルとフォルダ**
2. Studio Kit がリストにあれば、チェックを入れる

---

## データの保存場所

アプリのデータは以下に保存されます：

```
~/.studio-kit/
├── data.db         # データベース（投稿、設定など）
├── media/          # アップロードした素材
├── thumbnails/     # 自動生成されたサムネイル
└── subtitles/      # AI生成された字幕（SRT形式）
```

バックアップしたい場合は、このフォルダごとコピーしてください。

---

## 開発者向け

### 開発環境で起動

```bash
npm install
npm run tauri dev
```

### ビルド

```bash
npm run tauri build
```

### 技術スタック

- React + TypeScript + Tailwind CSS
- Rust + Tauri 2.0
- SQLite
- Claude API
